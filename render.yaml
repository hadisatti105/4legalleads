services:
  - type: web
    name: 4legalleads-app
    env: python
    branch: main
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 10000
